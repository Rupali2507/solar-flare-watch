FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app/ ./app
COPY data/ ./data
COPY detect_real_flares.py .

EXPOSE 8000

CMD ["sh", "-c", "python detect_real_flares.py && uvicorn app.main:app --host 0.0.0.0 --port 8000"]